#include "holberton.h"

/**
 * _isupper - checks for uppercase
 * @c: character used for uppercase
 *
 * Return: 1 if uppercase or 0 if not
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
